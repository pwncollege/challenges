HTTP header fields are extensible, so applications can define fields beyond the standardized set.
Add the custom field this service requires and recover the flag.
Run `/challenge/server`, then interact with it at `http://challenge.localhost/`.
