Headers are an open set.
A client may send any name it likes, and a server may require one that no browser would think to send on its own.
This service expects a header that appears in no ordinary request, and it names the one it wants.
Run `/challenge/server`, then interact with it at `http://challenge.localhost/`.
