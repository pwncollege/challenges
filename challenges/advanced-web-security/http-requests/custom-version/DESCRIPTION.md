The final token of an HTTP/1-style request line identifies the protocol version.
Ordinary clients generate this token from a version they support rather than accepting an arbitrary value.
Construct the request line this service expects and recover the flag.
Run `/challenge/server`, then interact with it at `http://challenge.localhost/`.
