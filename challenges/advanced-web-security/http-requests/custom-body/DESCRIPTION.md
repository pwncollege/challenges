`Content-Length` tells an HTTP/1.1 server exactly how many following bytes belong to the request body.
Set the length and contents this service expects and recover the flag.
Run `/challenge/server`, then interact with it at `http://challenge.localhost/`.
