An HTTP method is the first token of the request line, and servers can define extension methods beyond familiar ones such as `GET` and `POST`.
Send the method this service expects and recover the flag.
Run `/challenge/server`, then interact with it at `http://challenge.localhost/`.
