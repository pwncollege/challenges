Client address is only one piece of proxy metadata.
A backend may also trust forwarded authority when building absolute links.
Poison the admin's reset link across the proxy boundary, run `/challenge/victim`, and recover the flag.
Run `/challenge/server`, then interact with it at `http://challenge.localhost/`.
