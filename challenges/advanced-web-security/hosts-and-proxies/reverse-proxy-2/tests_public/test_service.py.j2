{%- extends "../../common/tests_public/service.py.j2" -%}

{% block assertions %}
assert response.status_code == 200
response = session.get(base_url + "/admin")
assert response.status_code == 403
assert "pwn.college{" not in response.text
{% endblock %}
