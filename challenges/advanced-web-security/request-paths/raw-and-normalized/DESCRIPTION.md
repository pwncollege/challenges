The original request target and the path consumed by an application are different representations.
A security decision over the first may not constrain routing with the second.
Exploit that disagreement and recover the flag.
Run `/challenge/server`, then interact with it at `http://challenge.localhost/`.
