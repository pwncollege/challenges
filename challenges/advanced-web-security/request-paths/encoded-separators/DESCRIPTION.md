Percent-decoding can create `/` and turn data inside one segment into path structure.
Exploit the resulting boundary change and recover the flag.
Run `/challenge/server`, then interact with it at `http://challenge.localhost/`.
