An intermediary and a backend HTTP stack can each decode the path once.
Trace the intermediate representation, then reach the backend's flag route.
Run `/challenge/server`, then interact with it at `http://challenge.localhost/`.
