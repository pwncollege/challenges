A proxy can replace a matched external prefix before forwarding a request.
Map the public path to the backend's flag route.
Run `/challenge/server`, then interact with it at `http://challenge.localhost/`.
