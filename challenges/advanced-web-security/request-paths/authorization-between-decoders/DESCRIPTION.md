A normalized path is safe to authorize only if no later component changes its structure.
Exploit an authorization check placed between two decoding stages.
Run `/challenge/server`, then interact with it at `http://challenge.localhost/`.
