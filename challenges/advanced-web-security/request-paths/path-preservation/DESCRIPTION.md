A reverse proxy can preserve the request path while sending it over a new connection.
Trace the path to the backend and find the route that releases the flag.
Run `/challenge/server`, then interact with it at `http://challenge.localhost/`.
