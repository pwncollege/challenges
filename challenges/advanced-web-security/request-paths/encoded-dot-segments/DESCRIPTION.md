Rejecting literal dot segments does not reject equivalent segments created by decoding.
Exploit the representation change and recover the flag.
Run `/challenge/server`, then interact with it at `http://challenge.localhost/`.
