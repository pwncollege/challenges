#!/bin/bash

FLAG=$(cat /flag)

ASM=$(mktemp).o
as -o $ASM <<< ".intel_syntax noprefix; main: mov rdi, 0; mov rax, 60; syscall"
ld -o /challenge/debug-me $ASM 2>/tmp/.init.log
rm $ASM

# Pass the flag as argv[1]
echo "set args $FLAG" >> /challenge/.gdb
