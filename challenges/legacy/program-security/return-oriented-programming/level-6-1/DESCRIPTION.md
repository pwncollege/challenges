Craft a ROP chain to obtain the flag, now with no syscall gadget!

Gadget-finding tools are expected here.
In this challenge image, `ROPgadget` and pwntools' `ROP` helper are available; `rp++` is not installed.
