Redirecting `echo` is handy for short pieces of text, but it becomes awkward when you need to revise a file or write several lines.
Terminal text editors let you work on a file interactively without leaving your shell.
This module introduces Nano, a small editor whose essential shortcuts are always shown on the screen.
