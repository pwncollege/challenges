#!/bin/bash

echo "bash: $(basename $0): command not found"
echo
fold -s <<< "It looks like you did not put quotes around the multi-word value in your variable assignment! This caused the shell to treat '$(basename $0)' as a command. Quote your multi-word values!"
