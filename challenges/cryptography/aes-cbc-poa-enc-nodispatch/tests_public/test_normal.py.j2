#!/usr/bin/env python3
import subprocess

p = subprocess.Popen(
    ["/challenge/worker.py"],
    stdin=subprocess.PIPE,
    stdout=subprocess.PIPE,
    text=True,
)
p.stdin.write("TASK: 00\n")
p.stdin.flush()
