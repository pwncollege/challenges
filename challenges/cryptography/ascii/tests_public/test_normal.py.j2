#!/usr/bin/env python3

import subprocess
import time
import sys

proc = subprocess.Popen(
    ["/challenge/run.py"],
    stdin=subprocess.PIPE,
    stdout=subprocess.PIPE,
    stderr=subprocess.PIPE,
    text=True,
)

output = proc.communicate(timeout=2)[0]
assert "You must interact with me directly" in output