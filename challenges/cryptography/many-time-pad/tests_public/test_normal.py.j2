#!/usr/bin/env python3
import subprocess

p = subprocess.Popen(
    ["/challenge/run.py"],
    stdin=subprocess.PIPE,
    stdout=subprocess.PIPE,
    text=True,
)
assert "Flag Ciphertext" in p.stdout.readline()
