#!/usr/bin/env python3
import subprocess

p = subprocess.Popen(
    ["/challenge/run.py"],
    stdin=subprocess.PIPE,
    stdout=subprocess.PIPE,
    text=True,
)

p.stdout.readline()
p.stdin.write("1\nA\n")
p.stdin.flush()
assert "Result:" in p.stdout.readline()
