#!/usr/bin/env python3
import subprocess

p = subprocess.Popen(
    ["/challenge/run.py"],
    stdin=subprocess.PIPE,
    stdout=subprocess.PIPE,
    text=True,
)

p.stdin.write("00\n")
p.stdin.flush()
assert "Ciphertext:" in p.stdout.readline()
