For each challenge in this module, a different standard program owned by `root` has its `suid` bit set.
That program was not designed to run with elevated privileges.

Your task is to misuse the program to read the flag.
Some of these tools can already read files directly.
Many of them cannot, and that is the point.

To understand how to use, and misuse, these programs, spend time with their `man` pages.
