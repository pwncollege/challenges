Recursive Wasm type groups validate related heap types together.
The bookkeeping problem is keeping both sides of a relationship wired consistently.

In this level, the harness gives you two tokens for a mutually linked pair.
Define `solve(api)` to build the modeled pair with both peer links and both tokens intact, then run `/challenge/run` with your solve file to get the flag.
