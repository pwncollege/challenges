Imported-wrapper code pointer table entries choose which compiled wrapper target runs.
Changing the entry can redirect the wrapper without changing the JavaScript import object.

In this level, the harness gives you a modeled wrapper and code pointer table.
Patch the wrapper entry to reach the win target, then run `/challenge/run` with your solve file to get the flag.
