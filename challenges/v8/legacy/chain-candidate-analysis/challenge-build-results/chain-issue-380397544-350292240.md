# chain-issue-380397544-350292240

Generated: 2026-06-04.

Verdict: **KEEP / challenge-backed**.

## Chain

- First stage: Chromium issue `380397544`, fixed by
  `0f2e4bf2b22ee367a02c9bc6b7ab26a1f4411f5d`, `[wasm] Fix comparison of
  relative type indexes`.
- Second stage: pre-signature-hash Wasm code-pointer table, fixed by
  `58f407806ad0ea83d8174dd701ba4b84c3cca14f`, `[sandbox][wasm] add signature
  checks to the code pointer table`.
- Challenge directory: `chain-issue-380397544-350292240`.
- Target revision:
  `93f2ac80b7bd296732576b7a8c37e45bc26f7eb3`
  (`0f2e4bf2b22ee367a02c9bc6b7ab26a1f4411f5d^`).

## Authenticity Checks

- The V8 patch file is empty.
- The build enables the V8 heap sandbox and uses the target's default
  WasmCodePointerTable state.
- The target is after CPT default enablement
  `bca5a5d291ae7877bcdbd185955bd63e962ef0f1`.
- The target is before the `b/380397544` first-stage fix.
- The target is before the CPT signature-check hardening fix.
- The exploit uses no `Sandbox.MemoryView`, `--sandbox-testing`, or
  memory-corruption helper API.

## Exploit Shape

The private exploit turns the `b/380397544` relative canonical-index confusion
from `regress-380397544.js` into helper-free `addrof`, caged `read32`, and
caged `write32`. It then reuses the authenticated CPT second-stage path:
corrupt a Wasm function reference's code-pointer handle, call through an
incompatible import wrapper before signature hashes are checked, gain native
read/write, and ROP to `/challenge/catflag`.

## Validation

```bash
nix develop -c pwnshop test chain-issue-380397544-350292240 --jobs 1 --timeout 300
```

Result:

```text
All tests passed (1 challenges, 2 testcases)
```

