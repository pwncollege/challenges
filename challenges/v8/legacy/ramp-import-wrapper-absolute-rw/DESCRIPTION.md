An imported-wrapper corruption can become a process-memory primitive once it is wrapped as absolute read/write.
The shape is different from the table-dispatch route, but the resulting contract is the same.

In this level, the harness gives you wrapper-level read and write operations.
Return an object with absolute `read32` and `write32` methods, then run `/challenge/run` with your solve file to get the flag.
