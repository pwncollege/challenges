An exact reference promises that the dynamic heap type is exactly the annotated type.
A subtype reference only promises that the value fits under that type.

In this level, the harness gives you one exact case and one subtype-compatible case.
Route each case through the matching API function, return the two route results in order, then run `/challenge/run` with your solve file to get the flag.
