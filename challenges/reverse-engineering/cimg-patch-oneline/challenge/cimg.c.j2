{%- import "../../common/cimg_lib.j2" as lib -%}
{%- set c = namespace() -%}
{{- lib.defaults(c, "patch-oneline") -}}
{{- lib.draw_magic(c, random, false) -}}
{%- set c.version = 3 -%}
{%- set c.has_dims = true -%}
{%- set c.color = true -%}
{%- set c.assert_ascii = true -%}
{%- set c.display = true -%}
{%- set c.framebuffer = true -%}
{%- set c.win = false -%}
{%- set _ = lib.assign_directives(c, random, ["RENDER_FRAME", "RENDER_PATCH"]) -%}
{%- include "../../common/cimg_body.c.j2" -%}
